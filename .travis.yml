language: android
android:
  components:
    - tools
    - build-tools-26.0.2
    - android-25
    - platform-tools
    - extra-android-support # because I'm use support library
    - extra-android-m2repository # because I'm use support library
  licenses:
    - '.+'

sudo: required

addons:
  sonarcloud:
    organization: "Walkandwork"
    token:
      secure: "d821ed7e7987c723b8f9ac26cbcff3afa79cd567"


jdk:
  - oraclejdk8

install: true

before_script:
  - chmod +x gradlew

script:
 - chmod +x ./gradlew
 - ./gradlew sonarqube
